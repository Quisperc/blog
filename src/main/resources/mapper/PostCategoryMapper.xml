<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.civer.blog.Mapper.PostCategoryMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into t_post_category(post_id, category_id, create_time, update_time)
        VALUES (#{postId},#{categoryId},NOW(),NOW())
    </insert>
        <insert id="insertIfNotExist" parameterType="map">
        INSERT INTO t_post_category (post_id, category_id, create_time, update_time)
        SELECT #{postId}, #{categoryId}, NOW(), NOW()
        FROM DUAL
        WHERE NOT EXISTS (
            SELECT 1 FROM t_post_category
            WHERE post_id = #{postId} AND category_id = #{categoryId}
        )
    </insert>

    <delete id="deleteByPostIds">
        DELETE FROM t_post_category WHERE post_id IN
         <foreach item="item" index="index" collection="postIds" open="(" separator="," close=")">
             #{item}
         </foreach>
    </delete>

    <select id="selectByPostIds" parameterType="list" resultType="cn.civer.blog.Model.DTO.PostCategoryDTO">
        SELECT id as id, post_id as postId, category_id as categoryId, create_time as createTime, update_time as updateTime
        FROM t_post_category
        WHERE post_id IN
        <foreach item="item" index="index" collection="postIds" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>

    <update id="update">
        UPDATE t_post_category
        SET category_id = #{categoryId},
            update_time = NOW()
        WHERE id = #{id}
    </update>
</mapper>